/*
Copyright © 2024 NAME HERE <EMAIL ADDRESS>
*/
package main

import (
	// "lowpm/cmd"
	"lowpm/libs/utils"
	// "fmt"
)

func main() {
	utils.PrintInfo("hello from lowpm")
	// cmd.Execute()
	// root :=	utils.GetCurrentUserRoot()
	// utils.PrintInfo(root)
	// Current returns the current user, or nil if not set.

	// defer os.RemoveAll(path)
	utils.PrintInfo("------- Done -------")
}
